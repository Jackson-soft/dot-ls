#pragma once

// 环形缓存

#include <cstddef>

namespace basis::common {
template<typename T>
class RingBuffer {
public:
    RingBuffer(std::size_t capacity) {}

    ~RingBuffer() {}
};
}  // namespace basis::common